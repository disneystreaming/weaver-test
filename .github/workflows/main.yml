name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: olafurpg/setup-scala@v7      
    - name: Compile and test
      run: sbt ci
    - run: echo Built $(cat version)
